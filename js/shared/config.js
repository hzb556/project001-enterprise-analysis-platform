/**
 * AI_FACTORY 配置
 */

// ---- 后端 API 地址 ----
const API_BASE = window.location.origin;  // 同源部署，自动适配

// ---- 应用配置 ----
const APP_CONFIG = {
    appName: '企业运营分析平台',
    appVersion: '8.0.0',
    maxFileSizeMB: 50,
    maxTotalSizeMB: 100,
    supportedFormats: ['.xlsx', '.xls'],
};
